# 0.4.46 完整验收清单（持续更新）

本清单保留2026-09-09总体优化方案及2026-09-10 RBAC方案的范围。当前未达到整体验收条件；局部测试通过不能代替HTTP/真机/性能证据。详细进度见IMPLEMENTATION_STATUS.md，部署事实见后端DEPLOYMENT_LOG.md。

## 环境与交付门禁

- [x] 前后端 `release/0.4.46`、测试服务最终程序集和测试小程序构建均有分支、哈希及部署台账记录。
- [x] 关键数据变更表迁移在adminet_test执行并回读字段；本轮无生产测试写入、无生产媒体覆盖。
- [x] 最新测试构建成功，产物使用https://shenzuyk.com/test-api；未上传，避免把未真机验证版本发布出去。
- [ ] 生产发布仍遵守：小程序审核通过通知，后端部署验证，用户发布小程序。
- [ ] 每阶段只保留必要程序/配置/业务小表回滚点，不完整备份大日志。
- [x] 构建、全套测试、类型检查、定向ESLint、BOM及diff检查通过，并有测试服务和微信模拟器运行证据。

## RBAC

- [x] 内置角色与旧身份映射、叠加身份策略和空角色拒绝策略已实现并有测试；存量用户正式迁移仍保持Shadow。
- [x] 角色允许取并集、用户禁止优先、过期覆盖失效、空角色明确拒绝均有测试。
- [ ] 读取/修改/删除数据范围分别计算；指定楼盘排除优先；房源创建人不被楼盘创建人替代。
- [ ] Shadow对未迁移用户双算并输出脱敏差异，不用于放行；Enforced仅在负向测试完成后接管。
  - [x] 本地只读模板映射、接管空角色拒绝、动作/范围表达式差异、脱敏日志测试通过；不写用户分配，不更改旧授权。
  - [ ] 测试服务真实请求日志、实际SQL命中集合对照及所有差异分类完成后，才评估Enforced。
- [ ] 超管可管理角色和用户例外，其他身份不能通过直接API绕过；根权限有防锁死保护。
- [x] 角色/用户权限保存有乐观锁和原因审计；盘源数据另有关键字段版本、冲突拒绝和恢复审计。
- [ ] 房东/维护关系、账号停用、手机号与角色变化使旧会话权限及时失效，跨实例/并发回填验证通过。
  - [x] 本地业务服务关系/等级/注销/手机号变化接入共享版本；批量去重，SQLite双实例缓存提交/回滚及并发旧回填测试通过。
  - [x] 账号最小字段独立查询已覆盖框架入口；测试库直接停用、注销或清手机号后，同一真实Token的HTTP请求均401。
  - [ ] 真实多实例/完整HTTP验收仍待完成；Legacy不提供新关系共享版本保证。
- [x] 角色管理、个人覆盖、到期设置、数据范围和有效权限来源预览已接入；微信模拟器已读取角色并渲染用户角色勾选项。
- [ ] 菜单、按钮、路由、API和数据范围一致；获得端入口不等于获得所有管理动作。
- [ ] 未授权动作有明确提示，数据权限变化后旧页面和在途结果不重新暴露已撤销数据。

## 数据边界与兼容

- [ ] AUTH-01：通用Page/List/Stats/Detail、经营配置、推广、媒体、批量、人员筛选、榜单全部做不同用户的正负向HTTP验证。
  - [x] 独立合成账号30项Enforced真实HTTP验证通过：楼盘、楼栋、房源、统计、批量列表、地图点位、媒体元数据/预览/关联/复制来源、账号状态及撤权。报告 `rbac-http-report-v7.json`。
  - [ ] 该报告使用测试PC Token；全部业务接口、微信APP和成功媒体文件流仍须覆盖。
- [ ] 房东标准路由和旧别名都不能越过本人范围；管理员兼房东保留获准多端。
- [ ] 撤销写权限后仍可查看获准数据，但单套/批量/同步全部均不能写入。
- [ ] 同步全部房源有一条无权时，在任何业务写入前拒绝；整批失败不留下部分修改。
- [ ] 数量、租金、户型、佣金、联系人电话只来自用户可见范围；电话禁止不能由其他DTO字段绕过。
- [ ] 公共预览不包含真实楼盘名称/Id/坐标/媒体；无区域中心时不使用楼盘均值。
- [ ] 无点位区域保留列表，地图不出现0,0/NaN；管理端有中心点补录入口或待办。
- [ ] 旧客户端未传新增参数/版本时仍可使用兼容路径，结果与新客户端一致的部分无回归。

## 观测与性能

- [x] 测试环境请求日志不含Authorization、Cookie、票据、正文和返回值；异常带TraceId。
  - [x] 测试服务诱饵请求验证：认证头、Cookie、查询票据、正文票据和旧返回正文均0命中，TraceId返回并出现在精简诊断行。
  - [ ] 生产配置/24小时增长和所有错误日志路径验证。
- [ ] LOG-02：成功读取采样、响应体限制真实生效；同流量24小时技术日志字节下降目标80%，不能删业务审计换收益。
- [ ] LOG-03：Docker/File/Nginx各有容量上限与真实轮转记录；历史删除需独立清单批准。
- [ ] 请求层记录路由模板、HTTP/业务码、耗时、字节数、SQL次数/耗时；无指标时不伪造为0。
- [ ] DB-01：媒体BelongId/FileType索引前后EXPLAIN与限量基准，上传/首帧/复制/挂载/删除写回归通过。
  - [x] adminet_test前后查询计划、数量及结果哈希一致性、脚本幂等验证通过。
  - [ ] 完整媒体写回归及生产索引审批。
  - [x] 同一索引临时表写入验证及97项本地媒体/批量测试通过，无真实媒体修改。
- [x] 测试库已按EXPLAIN验证房源筛选和活动明细候选索引，迁移脚本幂等，不改变房号后缀语义。
- [ ] QUERY-01：无距离场景走数据库稳定排序分页，结果/总数/排序与旧实现一致。
  - [x] 测试服务开启新分支，14组新旧回放连续两次一致，包括自动定位后只显示距离的列表。
  - [ ] 扩充权限范围/特殊筛选/分页并发数据变化与性能样本。
- [ ] QUERY-02：距离查询边界、四舍五入、无坐标、面议价格及筛选组合与旧实现一致。
- [x] 地图使用单map、轻量点位、全量命中和点选详情；模拟器真实加载431个点位，未合并气泡。
- [ ] CACHE-01：请求键含端、权限、筛选、位置、分享；在途去重与旧响应丢弃，失败不缓存为空成功。
  - [x] token/revision隔离旧401、旧成功响应、网络失败、重复token登录；房东store换号/换端/强刷回归28项通过。
  - [ ] 全页面请求键清查及iOS/安卓真机换号验收。
  - [x] MyAccessOutput授权版本参与前端签名；同Token权限版本变化会清缓存，昵称变化不会误清缓存。
- [x] 媒体本地缓存增加环境/会话隔离、在途去重、有限容量、TTL和文件存在性检查；12项单元测试通过。
- [ ] iOS/安卓媒体弱网、后台回收、重新下载和权限撤销真机验收。
- [ ] 字典/公共聚合/轻点/详情/媒体缓存有界且各有失效验证，退出换号不会跨用户回填。
- [ ] 相同数据/设备/网络/缓存条件下记录冷暖P50/P95/P99和错误率，达到约定目标而非仅构建通过。
  - [x] 测试API只读区域聚合20次顺序样本：0错误，P50约69.54ms、P95/P99约509.76ms；不是压力测试，报告见2026-09-14/performance-baseline-v1.json。

## 交互与UI

- [x] UI-01：现有页面已具备局部刷新与锚点保持实现；仍需微信真机长列表验收。
- [ ] 改动影响筛选/排序时条目处理明确；删除/新增同步地图、统计、全选与当前选中项。
- [x] 共享筛选、媒体、批量、浮层和选择工具已按权限接入；核心管理页面模拟器巡检通过。
- [ ] 三端图标、导航、字号、颜色和触控区域一致，无横向溢出/按钮被长文件名挤压/信息重叠。
- [x] 代码和模拟器巡检已验证核心滚动/浮层结构；真机手势仍需设备验收。
- [ ] 微信真机iOS/安卓验证媒体选择、长按菜单、下载/保存、扫码、地图与返回。
- [ ] Web/H5适用页面验证375/768/1024/1440宽度、焦点/键盘、对比度和reduced-motion。

## 必须保留的业务回归

- [ ] 匿名与游客同等预览；业务员申请资料完整；房东申请无需材料，管理员/维护人不能扫码变房东。
- [ ] 业务员仅可租，数量与点开列表一致；区域、0-10000租金、距离、类型、实时/热门/到期、宠物、特殊条件组合均保留。
- [ ] 楼层区间、02/5003/1A后缀、室厅卫0值、状态筛选与全选范围正确。
- [ ] 两种批量新增规则、递增默认0、预览排除重复、规则/预览切换不丢输入。
- [ ] 批量编辑空值保持、0为有效；媒体追加/替换/清空及封面含义不变。
- [ ] 管理员仅可删除本人创建，超管全量；级联提醒和事务正确，房东/维护人默认无删除。
- [ ] 多选媒体数量一致、单条失败后后续继续、封面回读/改名/删除/长按/保存均无回归。
- [ ] 公告、宠物、佣金区间、盘源更新人/联系人来源及显示位置符合既有确认。

## 待决策阶段（保留范围，不擅自实施）

- [ ] MEDIA-01：确认先做页内进度/重试还是跨页队列；需草稿、取消、失效与弱网真机验证。
- [ ] DATA-01：关键差异快照、超级管理员恢复、行锁冲突校验和恢复审计已实现；保留期、删除策略及批量恢复上限仍需业务确认。
- [ ] 历史日志删除、物理表重建、Redis/多实例、扩容和压力测试窗口单独确认，不附带生产操作。

## 现有证据范围

- 已有策略、SqlSugar SQLite范围和基础接口验证，详见进度记录；生产与真机验收未完成。
- 已运行的saveUser回读验证已恢复临时配置，不能据此认定Enforced下业务接口无越权。
- 暂无24小时日志增量、完整性能对比、全面负向HTTP或本轮真机证据。
- 测试服务已部署账号状态及媒体守卫，19项真实HTTP证据已补充；仍无全面负向HTTP、成功COS文件流及本轮微信真机证据，不能据此整体勾选通过。
- 2026-09-11本地续作：后端433通过/2既有跳过，前端300通过，类型检查与变更文件定向ESLint通过。全仓lint仍有1022错误，不能标成全仓通过。详见RBAC_MUTATION_AND_SHADOW_EVIDENCE.md。

## 2026-09-14 最新证据边界

- 后端最终门禁：464通过、2个既有交互测试跳过；前端43个测试文件、311项通过；测试小程序构建、类型检查、定向ESLint、BOM和diff检查通过。
- 测试环境：`https://shenzuyk.com/test-api` / `adminet_test` / `shenle-test-app`；生产服务、生产库和生产媒体未操作。
- 微信模拟器：地图、房源、工作台、我的、数据变更、角色权限及用户权限页面真实导航通过；console错误过滤为空；测试环境访问和角色数据真实回读通过。
- 外部发布门禁：iOS/安卓真机、弱网媒体和扫码验收需要真实设备；生产部署需要用户明确发布窗口；版本保留期、历史删除和批量恢复上限需要业务确认。以上项目保持未勾选，避免把缺少证据当成完成。
